--- a/wined3d/context.c
+++ b/wined3d/context.c
@@ -2488,7 +2488,7 @@
     WARN("Depth stencil format is not supported by WGL, rendering the backbuffer in an FBO\n");
 
     /* The currently active context is the necessary context to access the swapchain's onscreen buffers */
-    surface_load_location(context->current_rt, context, WINED3D_LOCATION_TEXTURE_RGB);
+    dg_surface_load_location(4, 2491, context->current_rt, context, WINED3D_LOCATION_TEXTURE_RGB);
     swapchain->render_to_fbo = TRUE;
     swapchain_update_draw_bindings(swapchain);
     context_set_render_offscreen(context, TRUE);
--- a/wined3d/device.c
+++ b/wined3d/device.c
@@ -226,7 +226,7 @@
     surface_prepare_map_memory(old);
     /* surface_load_location accepts an already valid CPU copy independently of
      * the current drawable. GPU-only copies require the exact recorded owner. */
-    if (FAILED(hr = surface_load_location(old, context, old->resource.map_binding))) return hr;
+    if (FAILED(hr = dg_surface_load_location(6, 229, old, context, old->resource.map_binding))) return hr;
     surface_modify_ds_location(old, old->resource.map_binding,
             old->ds_current_size.cx, old->ds_current_size.cy);
     return WINED3D_OK;
@@ -245,7 +245,7 @@
     }
     else if (old)
     {
-        if (FAILED(hr = surface_load_location(old, context, WINED3D_LOCATION_TEXTURE_RGB))) return hr;
+        if (FAILED(hr = dg_surface_load_location(6, 248, old, context, WINED3D_LOCATION_TEXTURE_RGB))) return hr;
         surface_modify_ds_location(old, WINED3D_LOCATION_TEXTURE_RGB,
                 old->ds_current_size.cx, old->ds_current_size.cy);
     }
@@ -381,7 +381,7 @@
         if (rt && rt->resource.format->id != WINED3DFMT_NULL)
         {
             if (flags & WINED3DCLEAR_TARGET && !is_full_clear(target, draw_rect, clear_rect))
-                surface_load_location(rt, context, rt->container->resource.draw_binding);
+                dg_surface_load_location(6, 384, rt, context, rt->container->resource.draw_binding);
             else
                 wined3d_surface_prepare(rt, context, rt->container->resource.draw_binding);
         }
@@ -4095,7 +4095,7 @@
             && src_rect.bottom == sub_resource->height)
         wined3d_texture_prepare_texture(texture, context, FALSE);
     else
-        surface_load_location(surface, context, WINED3D_LOCATION_TEXTURE_RGB);
+        dg_surface_load_location(6, 4098, surface, context, WINED3D_LOCATION_TEXTURE_RGB);
     wined3d_texture_bind_and_dirtify(texture, context, FALSE);
 
     wined3d_surface_upload_data(surface, gl_info, resource->format,
--- a/wined3d/drawprim.c
+++ b/wined3d/drawprim.c
@@ -643,7 +643,7 @@
         {
             if (state->render_states[WINED3D_RS_COLORWRITEENABLE])
             {
-                surface_load_location(target, context, target->container->resource.draw_binding);
+                dg_surface_load_location(8, 646, target, context, target->container->resource.draw_binding);
                 surface_invalidate_location(target, ~target->container->resource.draw_binding);
             }
             else
--- a/wined3d/surface.c
+++ b/wined3d/surface.c
@@ -29,6 +29,7 @@
 #include "config.h"
 #include "wine/port.h"
 #include "wined3d_private.h"
+#include "dg-wine-copy-diagnostics.h"
 #include "dg-wine-blit-usage.h"
 #ifdef VBOX_WITH_WINE_FIXES
 # include <float.h>
@@ -760,7 +761,7 @@
     if (surface->container->swapchain && surface->container->swapchain->front_buffer == surface->container)
     {
         context = context_acquire(device, surface);
-        surface_load_location(surface, context, surface->container->resource.draw_binding);
+        dg_surface_load_location(21, 763, surface, context, surface->container->resource.draw_binding);
         context_release(context);
     }
     else if (surface->container->resource.format_flags & (WINED3DFMT_FLAG_DEPTH | WINED3DFMT_FLAG_STENCIL))
@@ -825,9 +826,9 @@
 
     /* Make sure the locations are up-to-date. Loading the destination
      * surface isn't required if the entire surface is overwritten. */
-    surface_load_location(src_surface, context, src_location);
+    dg_surface_load_location(21, 828, src_surface, context, src_location);
     if (!surface_is_full_rect(dst_surface, dst_rect))
-        surface_load_location(dst_surface, context, dst_location);
+        dg_surface_load_location(21, 830, dst_surface, context, dst_location);
     else
         wined3d_surface_prepare(dst_surface, context, dst_location);
 
@@ -916,11 +917,11 @@
 
     /* Make sure the locations are up-to-date. Loading the destination
      * surface isn't required if the entire surface is overwritten. (And is
-     * in fact harmful if we're being called by surface_load_location() with
+     * in fact harmful if we're being called by dg_surface_load_location(21, 919, ) with
      * the purpose of loading the destination surface.) */
-    surface_load_location(src_surface, old_ctx, src_location);
+    dg_surface_load_location(21, 921, src_surface, old_ctx, src_location);
     if (!surface_is_full_rect(dst_surface, &dst_rect))
-        surface_load_location(dst_surface, old_ctx, dst_location);
+        dg_surface_load_location(21, 923, dst_surface, old_ctx, dst_location);
     else
         wined3d_surface_prepare(dst_surface, old_ctx, dst_location);
 
@@ -1185,7 +1186,7 @@
     else
     {
         surface_prepare_map_memory(surface);
-        surface_load_location(surface, context, surface->resource.map_binding);
+        dg_surface_load_location(21, 1188, surface, context, surface->resource.map_binding);
         surface_invalidate_location(surface, ~surface->resource.map_binding);
     }
 
@@ -1314,6 +1315,7 @@
 static void surface_download_data(struct wined3d_surface *surface, const struct wined3d_gl_info *gl_info,
         DWORD dst_location)
 {
+    dg_copy_present(); dg_copy_record(3, surface, dst_location);
     const struct wined3d_format *format = surface->resource.format;
     struct wined3d_bo_address data;
 
@@ -1704,7 +1706,7 @@
         wined3d_texture_prepare_texture(dst_surface->container, context, FALSE);
     else
     {
-        HRESULT hr = surface_load_location(dst_surface, context, WINED3D_LOCATION_TEXTURE_RGB);
+        HRESULT hr = dg_surface_load_location(21, 1707, dst_surface, context, WINED3D_LOCATION_TEXTURE_RGB);
         if (FAILED(hr))
         {
             context_release(context);
@@ -1849,7 +1851,7 @@
     }
     TRACE("Reloading because surface is dirty.\n");
 
-    surface_load_location(surface, context, location);
+    dg_surface_load_location(21, 1852, surface, context, location);
     surface_evict_sysmem(surface);
 }
 
@@ -2835,6 +2837,7 @@
 HRESULT CDECL wined3d_surface_map(struct wined3d_surface *surface,
         struct wined3d_map_desc *map_desc, const struct wined3d_box *box, DWORD flags)
 {
+    dg_copy_record(7, surface, flags);
     const struct wined3d_format *format = surface->resource.format;
     unsigned int fmt_flags = surface->container->resource.format_flags;
     struct wined3d_device *device = surface->resource.device;
@@ -2919,7 +2922,7 @@
         
         /* CPU-to-CPU copies also need loading when no GL context is needed.
          * The loader rejects GPU transfers without a valid current context. */
-        hr = surface_load_location(surface, context2, surface->resource.map_binding);
+        hr = dg_surface_load_location(21, 2922, surface, context2, surface->resource.map_binding);
         if (context2)
             context_release(context2);
         if (FAILED(hr))
@@ -3024,6 +3027,7 @@
 
 HRESULT CDECL wined3d_surface_getdc(struct wined3d_surface *surface, HDC *dc)
 {
+    dg_copy_record(6, surface, WINED3D_LOCATION_DIB);
     HRESULT hr;
     struct wined3d_device *device = surface->resource.device;
     struct wined3d_context *context = NULL;
@@ -3046,7 +3050,7 @@
     {
         if (surface->flags & SFLAG_CLIENT)
         {
-            if (FAILED(hr = surface_load_location(surface, context, WINED3D_LOCATION_SYSMEM)))
+            if (FAILED(hr = dg_surface_load_location(21, 3049, surface, context, WINED3D_LOCATION_SYSMEM)))
                 goto done;
             surface_release_client_storage(surface);
         }
@@ -3059,7 +3063,7 @@
             surface->resource.map_binding = WINED3D_LOCATION_DIB;
     }
 
-    if (FAILED(hr = surface_load_location(surface, context, WINED3D_LOCATION_DIB)))
+    if (FAILED(hr = dg_surface_load_location(21, 3062, surface, context, WINED3D_LOCATION_DIB)))
         goto done;
     surface_invalidate_location(surface, ~WINED3D_LOCATION_DIB);
 
@@ -3112,7 +3116,7 @@
         if (device->d3d_initialized)
             context = context_acquire(device, NULL);
 
-        hr = surface_load_location(surface, context, surface->resource.map_binding);
+        hr = dg_surface_load_location(21, 3115, surface, context, surface->resource.map_binding);
         if (context)
             context_release(context);
         if (FAILED(hr))
@@ -3128,6 +3132,7 @@
 static HRESULT read_from_framebuffer(struct wined3d_surface *surface,
         struct wined3d_context *old_ctx, DWORD dst_location)
 {
+    dg_copy_present(); dg_copy_record(2, surface, dst_location);
     struct wined3d_device *device = surface->resource.device;
     const struct wined3d_gl_info *gl_info;
     struct wined3d_context *context = old_ctx;
@@ -3793,11 +3798,11 @@
     /* Save the source before loading the destination: without FBOs they may
      * share the physical backbuffer. Keep texture_load's color-key conversion. */
     wined3d_texture_load(src_surface->container, context, FALSE);
-    if (FAILED(hr = surface_load_location(src_surface, context, WINED3D_LOCATION_TEXTURE_RGB)))
+    if (FAILED(hr = dg_surface_load_location(21, 3796, src_surface, context, WINED3D_LOCATION_TEXTURE_RGB)))
         goto done;
     if (alpha_test || !surface_is_full_rect(dst_surface, dst_rect_in))
     {
-        if (FAILED(hr = surface_load_location(dst_surface, context,
+        if (FAILED(hr = dg_surface_load_location(21, 3800, dst_surface, context,
                 dst_surface->container->resource.draw_binding)))
             goto done;
     }
@@ -4582,7 +4587,7 @@
 
     if (surface->locations & (WINED3D_LOCATION_RB_MULTISAMPLE | WINED3D_LOCATION_RB_RESOLVED))
     {
-        if (FAILED(hr = surface_load_location(surface, context, WINED3D_LOCATION_TEXTURE_RGB)))
+        if (FAILED(hr = dg_surface_load_location(21, 4585, surface, context, WINED3D_LOCATION_TEXTURE_RGB)))
             return hr;
     }
 
@@ -4689,7 +4694,7 @@
             /* Performance warning... */
             FIXME("Downloading RGB surface %p to reload it as sRGB.\n", surface);
             surface_prepare_map_memory(surface);
-            surface_load_location(surface, context, surface->resource.map_binding);
+            dg_surface_load_location(21, 4692, surface, context, surface->resource.map_binding);
         }
     }
     else
@@ -4700,7 +4705,7 @@
             /* Performance warning... */
             FIXME("Downloading sRGB surface %p to reload it as RGB.\n", surface);
             surface_prepare_map_memory(surface);
-            surface_load_location(surface, context, surface->resource.map_binding);
+            dg_surface_load_location(21, 4703, surface, context, surface->resource.map_binding);
         }
     }
 
@@ -4709,7 +4714,7 @@
         WARN("Trying to load a texture from sysmem, but no simple location is valid.\n");
         /* Lets hope we get it from somewhere... */
         //surface_prepare_system_memory(surface);
-        //surface_load_location(surface, context, WINED3D_LOCATION_SYSMEM);
+        //dg_surface_load_location(21, 4712, surface, context, WINED3D_LOCATION_SYSMEM);
         // JHFIX: doesn't work, only causes stack overflow
         return WINED3DERR_INVALIDCALL;
     }
@@ -4737,7 +4742,7 @@
             surface->resource.map_binding = WINED3D_LOCATION_SYSMEM;
 
         surface_prepare_map_memory(surface);
-        surface_load_location(surface, context, surface->resource.map_binding);
+        dg_surface_load_location(21, 4740, surface, context, surface->resource.map_binding);
         surface_remove_pbo(surface, gl_info);
     }
 
@@ -5245,6 +5250,7 @@
         struct wined3d_surface *src_surface, const RECT *src_rect, DWORD flags,
         const WINEDDBLTFX *fx, enum wined3d_texture_filter_type filter)
 {
+    dg_copy_record(5, dst_surface, flags); dg_copy_record(8, src_surface, flags);
     const struct wined3d_box dst_box = {dst_rect->left, dst_rect->top, dst_rect->right, dst_rect->bottom, 0, 1};
     int bpp, srcheight, srcwidth, dstheight, dstwidth, width;
     const struct wined3d_format *src_format, *dst_format;
@@ -6088,7 +6094,7 @@
                         if (!wined3d_resource_is_offscreen(&dst_surface->container->resource))
                         {
                             struct wined3d_context *context = context_acquire(device, dst_surface);
-                            surface_load_location(dst_surface, context, dst_surface->container->resource.draw_binding);
+                            dg_surface_load_location(21, 6091, dst_surface, context, dst_surface->container->resource.draw_binding);
                             context_release(context);
                         }
                         return WINED3D_OK;
--- a/wined3d/swapchain.c
+++ b/wined3d/swapchain.c
@@ -149,6 +149,7 @@
         return WINED3DERR_INVALIDCALL;
     }
 
+    dg_copy_present();
     wined3d_cs_emit_present(swapchain->device->cs, swapchain, src_rect,
             dst_rect, dst_window_override, dirty_region, flags);
 
@@ -318,7 +319,7 @@
         if (backbuffer->resource.multisample_type)
         {
             location = WINED3D_LOCATION_RB_RESOLVED;
-            surface_load_location(backbuffer, context, location);
+            dg_surface_load_location(22, 321, backbuffer, context, location);
         }
 
         context_apply_fbo_state_blit(context, GL_READ_FRAMEBUFFER, backbuffer, NULL, location);
@@ -523,14 +524,14 @@
      */
     if (!swapchain->render_to_fbo && render_to_fbo && wined3d_settings.offscreen_rendering_mode == ORM_FBO)
     {
-        surface_load_location(back_buffer, context, WINED3D_LOCATION_TEXTURE_RGB);
+        dg_surface_load_location(22, 526, back_buffer, context, WINED3D_LOCATION_TEXTURE_RGB);
         surface_invalidate_location(back_buffer, WINED3D_LOCATION_DRAWABLE);
         swapchain->render_to_fbo = TRUE;
         swapchain_update_draw_bindings(swapchain);
     }
     else
     {
-        surface_load_location(back_buffer, context, back_buffer->container->resource.draw_binding);
+        dg_surface_load_location(22, 533, back_buffer, context, back_buffer->container->resource.draw_binding);
     }
 
     if (swapchain->render_to_fbo)
@@ -623,7 +624,7 @@
 
     TRACE("Copying surface %p to screen.\n", front);
 
-    surface_load_location(front, NULL, WINED3D_LOCATION_DIB);
+    dg_surface_load_location(22, 626, front, NULL, WINED3D_LOCATION_DIB);
 
     src_dc = front->hDC;
     window = swapchain->win_handle;
--- a/wined3d/texture.c
+++ b/wined3d/texture.c
@@ -818,7 +818,7 @@
 
     surface_prepare_map_memory(surface);
     context = context_acquire(surface->resource.device, NULL);
-    surface_load_location(surface, context, surface->resource.map_binding);
+    dg_surface_load_location(23, 821, surface, context, surface->resource.map_binding);
     context_release(context);
     surface_invalidate_location(surface, ~surface->resource.map_binding);
 }
--- a/wined3d/wined3d_private.h
+++ b/wined3d/wined3d_private.h
@@ -3436,3 +3436,6 @@
 BOOL EnumDisplaySettingsA95(LPCSTR lpszDeviceName, DWORD iModeNum, DEVMODEA *lpDevMode);
 
 #endif
+
+HRESULT dg_surface_load_location(unsigned int, unsigned int, struct wined3d_surface *, struct wined3d_context *, DWORD);
+void dg_copy_present(void);
